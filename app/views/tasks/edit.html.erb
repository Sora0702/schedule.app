<div class="container">
  <h3>予定を編集してください</h3>

  <%= render "shared/error", obj:@task %>

  <%= form_with model: @task do |f| %>
    <div class="row Row">
      <div class="col-12">
        <%= f.label :title ,"タイトル",class: "form-label" %>
        <%= f.text_field :title, class: "form-control" %>
      </div>
      <div class="col-6">
        <%= f.label :start_day ,"開始日", class: "form-label" %>
        <%= f.date_field :start_day, class: "form-control" %>
      </div>
      <div class="col-6">
        <%= f.label :finish_day ,"終了日", class: "form-label" %>
        <%= f.date_field :finish_day, class: "form-control" %>
      </div>
      <div class="col-12">
        <%= f.check_box :all_day, class: "form-check-input" %>
        <label class="form-check-label">終日の場合はチェックを入れます。</label>
      </div>
      <div class="col-12">
        <%= f.label :memo ,"スケジュールメモ", class: "form-label" %>
        <%= f.text_area :memo, class: "form-control" %>
      </div>

      <div class="Form-buttons">
        <%= link_to "キャンセル", :tasks, class: "btn btn-ssecondary" %>
        <%= f.submit "保存", class: "btn btn-primary" %>
      </div>
    </div>
  <% end %>
</div>